# explicitly bind the CRA dev server so it never picks up a shared PORT environment
PORT=3000

# backend endpoint (when not using the proxy field in package.json)
REACT_APP_API_URL=http://localhost:3030
